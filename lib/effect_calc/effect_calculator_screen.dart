import 'package:flutter/material.dart';

class EffectCalculatorScreen extends StatelessWidget {
  const EffectCalculatorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Effect Calculator"),
    );
  }
}
