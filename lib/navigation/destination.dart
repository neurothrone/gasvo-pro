enum Destination {
  home("Home"),
  settings("Settings"),
  gasPipeCalculator("Gas Pipe Calculator"),
  effectCalculator("Effect Calculator");

  const Destination(this.title);

  final String title;
}
